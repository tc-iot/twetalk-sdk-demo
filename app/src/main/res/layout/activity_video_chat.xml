<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/black">

    <!-- 相机预览层（全屏） -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 顶部状态栏 -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#80000000"
        android:padding="@dimen/video_top_bar_padding"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        
        <!-- 连接状态 -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipConnectionStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/disconnected"
            android:textColor="@color/white"
            app:chipBackgroundColor="@color/error_red"
            app:chipIcon="@drawable/ic_disconnected"
            app:chipIconTint="@color/selector_icon_tint_white" />
    </LinearLayout>

    <!-- 对话文本显示区域 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewMessages"
        android:layout_width="0dp"
        android:layout_height="@dimen/video_message_list_height"
        android:background="#00000000"
        android:padding="@dimen/video_message_list_padding"
        tools:listitem="@layout/item_chat_message"
        app:layout_constraintBottom_toTopOf="@id/controlPanel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 底部控制按钮 -->
    <LinearLayout
        android:id="@+id/controlPanel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#80000000"
        android:padding="@dimen/video_control_panel_padding"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 切换摄像头按钮 -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabSwitchCamera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/video_fab_margin_end"
            android:contentDescription="切换摄像头"
            app:fabSize="normal"
            app:backgroundTint="@color/white"
            app:tint="@color/primary_blue"
            app:srcCompat="@drawable/ic_switch_camera"/>

        <!-- 挂断按钮 -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabEndCall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="结束通话"
            app:fabSize="normal"
            app:backgroundTint="@color/error_red"
            app:tint="@color/white"
            app:srcCompat="@drawable/ic_end_call"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
